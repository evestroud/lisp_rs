workspace = { members = ["lisp_rs_core", "lisp_rs_macro", "macro_test"] }
[package]
name = "lisp_rs"
version = "0.1.0"
edition = "2021"

[lib]
crate_type = ["cdylib", "lib"]

[target.'cfg(target_arch = "x86_64")'.dependencies]
rustyline = "10.0.0"
pico-args = "0.5.0"

[dependencies]
wasm-bindgen = "0.2.86"
lisp_rs_core = { path = "lisp_rs_core" }
